language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t jtgriffi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jtgriffi/docker-react npm run test -- --coverage